[project]
name = "dronelogparsers"
version = "0.1.0"
description = "Custom parser for open-dronelog software to support PX4 ulog and betaflight logfiles"
requires-python = ">=3.14"
dependencies = [
    "pyulog>=1.2.2",
    "scipy>=1.17.1",
]
